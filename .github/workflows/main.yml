name: Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  check-dist:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/github-script@v6
      - name: View context attributes
        with:
          script: console.log("testing123", context)
