name: Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  check-dist:
    runs-on: ubuntu-latest

    steps:
      - uses: benwells/github-script@main
        with:
          script: console.log("testing123", context)
